apiVersion: v2
name: cgv-mysql
description: bitnami mysql 래퍼(벤더링) — booking 예매 원장 DB
type: application
version: 0.1.0
appVersion: "9.4.0"
dependencies:
  - name: mysql
    version: 14.0.3
    repository: https://charts.bitnami.com/bitnami
# Chart.lock 은 `helm dependency build` 로 만든 것. charts/mysql-14.0.3.tgz 실물과 짝이다 — 둘 중 하나만 갱신하면 안 된다.
