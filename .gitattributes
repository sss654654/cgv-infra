# 이 저장소의 파일은 리눅스 노드에서 실행·적용된다. Windows에서 체크아웃·복사해도
# CRLF가 섞이지 않게 고정한다(셔뱅에 CR이 붙으면 `env: 'bash\r': No such file or directory`).
* text=auto eol=lf
*.sh text eol=lf
*.yaml text eol=lf
*.yml text eol=lf
*.md text eol=lf
