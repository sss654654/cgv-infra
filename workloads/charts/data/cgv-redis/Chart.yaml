apiVersion: v2
name: cgv-redis
description: CGV Redis 래퍼 — bitnami redis(Sentinel HA)를 dependency로 품는다. 비밀번호는 중앙 workloads/manifests/secrets/의 SealedSecret(redis-secret)이 배달한다.
type: application
version: 0.1.0
appVersion: "7.4"
dependencies:
  - name: redis
    version: "~27.0.15"
    repository: https://charts.bitnami.com/bitnami
